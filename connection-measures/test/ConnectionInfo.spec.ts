
describe("ConnectionInfo", () => {
  it("should measure the connection speed", () => {
    // TODO write tests
    const input = 1;
    const result = input * 2;
    const expectedResult = 2;
    expect(result).toEqual(expectedResult);
  });

});
